-- Impression schema

-- !Ups

CREATE TABLE IMPRESSION (
    ID integer NOT NULL AUTO_INCREMENT,
    PLACEMENT varchar(6) NOT NULL,
    BIDDER varchar(255) NOT NULL,
    TIMEVAL TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
);

-- !Downs

DROP TABLE IMPRESSION;